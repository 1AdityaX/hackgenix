# better-auth secret (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET="generate-a-random-secret-at-least-32-chars"
BETTER_AUTH_URL="http://localhost:5173"

# Also set this for the client-side auth client
VITE_BETTER_AUTH_URL="http://localhost:5173"

# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# MongoDB connection
MONGODB_URI="mongodb://localhost:27017"
MONGODB_DB_NAME="hackgenix"

# URL of the Python RAG server (used by the SvelteKit API proxy)
PRIVATE_RAG_SERVER_URL="http://localhost:8000"
