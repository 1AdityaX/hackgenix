services:
  mongodb:
    image: mongo:7
    container_name: hackgenix-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: hackgenix
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
